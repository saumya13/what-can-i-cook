// import { createClient } from "@supabase/supabase-js";

// const DB_URL = import.meta.env.VITE_SUPABASE_URL;
// const DB_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;

// export async function downloadImage(imageURL: string) {
//   const supabase = createClient(DB_URL, DB_KEY);

//   const { data, error } = supabase.storage.from("recipes").download(imageURL);
// }
